.page-header
  %h1 HEROES OF THE STORM

%p.lead
  Hero build repository

.col-lg-3
  %img{data: {src: 'holder.js/95%x64'}}
  %h2 Assassins
  = render 'list', heroes: @heroes.where(specialization: 'assassin')
.col-lg-3
  %img{data: {src: 'holder.js/95%x64'}}
  %h2 Warriors
  = render 'list', heroes: @heroes.where(specialization: 'warrior')
.col-lg-3
  %img{data: {src: 'holder.js/95%x64'}}
  %h2 Supports
  = render 'list', heroes: @heroes.where(specialization: 'support')
.col-lg-3
  %img{data: {src: 'holder.js/95%x64'}}
  %h2 Specialists
  = render 'list', heroes: @heroes.where(specialization: 'specialist')
